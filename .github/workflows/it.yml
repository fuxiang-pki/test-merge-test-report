name: Integration Test
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    name: Run Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - testPackage: elements.connector
            moduleName: elements-connector
            testInclude: "-Dtest=/**/*Test1"
          - testPackage: extapi.FragmentsIT
            moduleName: test1
            testInclude: "-Dtest=/**/*Test2"
      fail-fast: false

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Set up Maven
      uses: stCarolas/setup-maven@v4.2
      with:
        maven-version: 3.6.3
    - name: Set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: maven
    - name: Build with Maven
      run: |
         mvn test --batch-mode --fail-at-end $TEST_INCLUDE
    - name: Publish Test Report
      if: ${{ always() }}
      uses: scacap/action-surefire-report@v1
